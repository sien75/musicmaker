/// <reference types="react" />
import { ControllerProps } from '../_types';
declare const normalController: ({ tone, scheduled, scheduled: { should, current, total }, onToSchedule, }: ControllerProps) => JSX.Element;
export default normalController;
